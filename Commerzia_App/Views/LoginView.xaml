<Page x:Class="Commerzia_App.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      xmlns:vm="clr-namespace:Commerzia_App.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024"
      Title="Login"
      FontFamily="Montserrat">
    <Page.DataContext>
        <vm:LoginViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1.3*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{Binding Color, Source={StaticResource PrimaryBrandBrush}}" Offset="0.0" />
                    <GradientStop Color="#2A60EE" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <fa:IconImage Icon="Shapes" Foreground="White" Width="80" Height="80" Opacity="0.5" Margin="0,0,0,20"/>
                <TextBlock Text="Good design is obvious." 
                           FontSize="24" FontWeight="Bold" Foreground="White" 
                           TextWrapping="Wrap" TextAlignment="Center" Margin="40,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Background="{StaticResource SurfaceBrush}">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400" Margin="40">

                <TextBlock Text="Welcome back!" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                <TextBlock Text="Please enter your credentials to access your workspace." 
                           FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" 
                           Margin="0,0,0,40" TextWrapping="Wrap"/>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Username" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource CustomTextBoxStyle}" Tag="moises_05" 
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontWeight="SemiBold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="Password" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid>
                        <PasswordBox x:Name="txtPassword" Style="{StaticResource CustomPasswordBoxStyle}" />
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Show Password">
                            <fa:IconImage Icon="Eye" Width="16" Height="16" Foreground="{StaticResource TextSecondaryBrush}" />
                        </Button>
                    </Grid>
                </StackPanel>

                <Grid Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox Content="Remember me" Style="{StaticResource ModernSwitchStyle}" Grid.Column="0" />

                    <TextBlock Grid.Column="1" VerticalAlignment="Center">
                        <Hyperlink Foreground="{StaticResource PrimaryBrandBrush}" TextDecorations="None" FontWeight="SemiBold">
                            Forgot password?
                        </Hyperlink>
                    </TextBlock>
                </Grid>

                <Button Content="Sign In" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,40"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=txtPassword}"/>

                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Separator Grid.Column="0" Background="{StaticResource DividerBrush}" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="OR CONTINUE WITH" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" Margin="16,0"/>
                    <Separator Grid.Column="2" Background="{StaticResource DividerBrush}" VerticalAlignment="Center" />
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Height="48" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <fa:IconImage Icon="Google" Foreground="#DB4437" Width="18" Margin="0,0,8,0"/>
                                        <TextBlock Text="Google" FontWeight="Medium" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2" Height="48" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <fa:IconImage Icon="Github" Foreground="#333" Width="18" Margin="0,0,8,0"/>
                                        <TextBlock Text="GitHub" FontWeight="Medium" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

            </StackPanel>
        </Grid>
    </Grid>
</Page>